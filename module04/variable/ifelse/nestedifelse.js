//if-else inside another if-else.

let age = 17;
if (age < 18) {
    if (age > 15) console.log("betweent 15 to 17 "); //no neet to{} because of this lineis pert of else if;
    else console.log("Not eligible "); //no neet to{} because of this lineis pert of else;
}