//Used for multiple conditions.

let age = 17;
if (age >= 18) console.log("eligible"); //no neet to{} because of this lineis pert of if;
else if (age >= 15) console.log("betweent 15 to 17 "); //no neet to{} because of this lineis pert of else if;
else console.log("Not eligible "); //no neet to{} because of this lineis pert of else;